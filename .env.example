# S3 Configuration
# For LocalStack, use the following settings:
S3_ENDPOINT_URL=http://localhost:4566
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test
S3_BUCKET_NAME=parquet-data-bucket

# For real AWS S3, comment out S3_ENDPOINT_URL and set real credentials:
# S3_ENDPOINT_URL=
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key
# S3_BUCKET_NAME=your-bucket-name

# Application Configuration
TARGET_DATA_SIZE_MB=10
BLOCK_SIZE_MB=1
NUM_RECORDS=100000

# Writer Configuration
# Options: local, s3_streaming
# - local: Write to local filesystem first, then upload to S3 (default for backward compatibility)
# - s3_streaming: Write directly to S3 using PyArrow's streaming writer (recommended)
WRITER_TYPE=s3_streaming
